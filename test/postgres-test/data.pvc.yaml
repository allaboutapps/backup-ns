apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: postgres-test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-hostpath-sc
